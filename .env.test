# Test Environment Configuration
# This file is used when running tests with GO_ENV=test

# Environment
GO_ENV=test

# Test Database Configuration
# Uses a dedicated test database to avoid interfering with development data
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/kendalls_nails_test?sslmode=disable

# Server Configuration
PORT=8080

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174

# Auth0 Configuration (test values)
AUTH0_DOMAIN=test.auth0.com
AUTH0_AUDIENCE=https://api.test.com

# AWS Configuration (not needed for tests, but included for completeness)
AWS_REGION=us-east-1
AWS_S3_BUCKET=kendalls-nails-uploads-test
AWS_ACCESS_KEY_ID=test-key
AWS_SECRET_ACCESS_KEY=test-secret

# Logging
LOG_LEVEL=debug
